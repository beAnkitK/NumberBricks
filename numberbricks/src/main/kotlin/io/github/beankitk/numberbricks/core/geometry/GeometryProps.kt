package io.github.beankitk.numberbricks.core.geometry

/**
 * Defines the immutable geometry properties that describe digit geometry.
 *
 * Provides the shared properties used to construct and control the overall geometry
 * of a digit. These properties define grid-level characteristics such as cell sizing,
 * spacing, and other structural parameters that influence how bricks are arranged
 * and rendered.
 *
 * Acts as the source of truth for digit geometry and is consumed by geometry
 * builders and providers during construction. Implementations must be immutable.
 */
interface GeometryProps {

}